plugins {
    id "com.github.c64lib.retro-assembler" version "1.5.3"
}

repositories {
    mavenCentral()
}

retroProject {
    dialect = "KickAssembler"
    dialectVersion = "5.22"
    libDirs = [".ra/deps/andrzejsliwa", ".ra/deps/c64lib"]

    libFromGitHub "c64lib/common", "0.3.0"
    libFromGitHub "c64lib/chipset", "0.3.0"
    libFromGitHub "andrzejsliwa/64spec", "master"
}

